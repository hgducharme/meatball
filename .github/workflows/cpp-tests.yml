name: C++ CI

on:
  push:
    branches: [ "dev/**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: make tests
      run: make tests
